# Copy to .env and fill in for your environment.

DATABASE_URL="file:./dev.db"

# The real deployed origin (e.g. https://scrambles.yourdomain.com). Used to
# resolve absolute URLs for the generated Open Graph share images — without
# it, shared links fall back to localhost in their link-preview image.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
